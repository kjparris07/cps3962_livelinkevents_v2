export type Venue = {
    venue_id: number,
    name: string,
    street: string,
    city: string,
    state: string,
    zipcode: string,
    organizer_id: number,
    capacity: number,
    map: string
}